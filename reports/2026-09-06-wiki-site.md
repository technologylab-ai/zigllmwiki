# Static wiki publication checks — 2026-09-06

The public wiki derives its documents from canonical repository files.
The [site guide](../docs/wiki-site.md) describes browsing and maintenance.
The [publication workflow](../.github/workflows/pages.yml) records each deployed commit.

## Development checks

The input tree started at `46ea228481cd1edcbb6cba3e664539d9da07c7fd` with reviewed website and documentation edits.
These checks used development inputs, not a clean publication revision.
The site adds no Zig implementation or runtime claim.

The host was an Apple M3 Max, model Mac15,9, with 16 logical CPUs and 128 GiB memory.
The operating system was macOS 26.6.2, build 25G83, Darwin 25.6.0, arm64.
The compiler was exact Zig 0.16.0 through `/Users/rs/bin/zig`.
Python was 3.9.6.

| Gate | Result |
| --- | --- |
| `zig build verify --summary all` | 87/87 steps; 73 passing tests and nine platform skips. |
| Command regression tests | 28/28 passed. |
| Retrieval policy | 25 queries; MRR 0.98, hit@3 1.0, recall@5 0.96. |
| Full wiki lint | Zero issues across 51 pages and 78 source records. |
| Builder fixtures | 14/14 passed. |
| Browser smoke checks | 12/12 passed. |

The lint wrapper repeated the complete verifier successfully.
Tracked hashes remained unchanged during the native gate.
The runner released its Mac reservation after every child process stopped.

The browser checks covered search, multiword typing, aliases, headings, source metadata, and unchanged proof text.
The graph checks covered pointer selection, zoom, evidence layers, neighborhood focus, and list navigation.
The adversarial fixture passed through the real reader with intercepted test data.
Script, iframe, event-handler, and JavaScript-link payloads remained inert.
Invalid paths and missing documents produced reader errors.
Home, reader, and graph views fit a 390-pixel viewport without horizontal page overflow.
Desktop and mobile screenshots received a visual review.

The first artifact contained 187 documents and 1,366 directed, typed references.
The graph combines duplicate pairs for display and gives evidence references precedence.
Later documentation updates can change document and connection counts.
The homepage counts 50 wiki files; the verifier also counts the root index as a page.

The artifact contains an explicit set of site assets and generated data.
The builder checks vendor hashes, proof registrations, symlinks, output boundaries, and input limits.
The builder preserves original document hashes and captured source metadata.
The development artifact carries `dirty: true`.
Clean publication artifacts record their exact commit and carry `dirty: false`.

## First publication

The [first Pages deployment](https://github.com/technologylab-ai/zigllmwiki/actions/runs/34035331551) succeeded at `f5e0a22798a677bcae196781f79e0e1fca99fc2f`.
The clean artifact contained 188 documents and 1,371 typed references.
All 12 browser checks passed against the public URL in Chrome 152.0.7977.76.
Four additional checks passed across the HTTP site, wiki, and existing Tailscale reader.
Those checks exercised viewing links and the corrected HTTP path validation.

The [HTTP deployment](https://github.com/technologylab-ai/zig-http/actions/runs/34035425137) published viewing links at `2b41a0801a655f02223a9ece2e8ebddfffed64f7`.
Pinned evidence URLs retained their original revisions.

The maintained Linux wrapper verified the exact pushed wiki commit on `omarx1`.
All 248 streamed files matched their Git blobs.
The environment was x86_64 Linux 7.1.9-arch1-2, glibc 2.44, Python 3.14.7, and Zig 0.16.0.
The compiler path was `/home/rs/.local/share/mise/installs/zig/0.16.0/bin/zig`.
Its SHA-256 was `2317bbb91798556d9d0f38aabdac23db83f0979b25f767259ae474546724087c`.
The gate passed all 87 steps, with 74 tests and eight platform skips.
All 28 command tests, 14 builder tests, and the 25-query retrieval policy passed.
Focused lint found zero issues.
The runner removed its temporary checkout and released its reservation at 13:16:47 UTC.

The Linux source archive SHA-256 was `2c74438e28d8df7cc07d07c9f4c88f985e308f60d0bc4e711b992c9acbb0b076`.
The gate log SHA-256 was `9e75279fe8993f8bfed4b04af7d43e146940101db9b316df91a8586766242f27`.
Two orchestration attempts stopped before Zig execution; the ignored packet preserves both attempts.
These results cover the first static graph, before the requested animation update.

## Graph interaction update

The user requested animation, click centering, all layers enabled, and an expanded matching-note list.
The graph computes finite force targets and interpolates positions over 850 milliseconds.
The selected node moves to the center while neighboring nodes rearrange.
The graph stops its animation frames after settling.
Reduced motion applies the same target immediately.
Cleanup cancels active frames, event handlers, and the resize observer.

All 13 development browser checks passed in Chrome 152.0.7977.76 with no uncaught exceptions.
The checks observed changing positions during startup and selection.
The selected node finished at the exact SVG viewport center on both axes.
All four layers started enabled, and the list started expanded.
Keyboard selection, explicit neighborhood filtering, history, reduced motion, and cleanup passed.
The broader reader, provenance, sanitization, and mobile checks also passed.

The [graph deployment](https://github.com/technologylab-ai/zigllmwiki/actions/runs/34036135180) succeeded at `7cf716b239cd78801a9685fee681c22546d9993a`.
All 13 checks also passed against that live publication with browser caching disabled.
The browser harness now waits for completed rendering instead of accepting a loading marker.
The earlier cold-request test exposed that harness race; the corrected wait passed.
No application failure or runtime proof change resulted from the race.

## Scope

The browser displays complete canonical proof files and does not execute them.
Existing platform claims retain their recorded environments and limits.
No Windows workflow or new performance measurement accompanied this website change.
M3-006 remains postponed, and unfinished M4 work remains queued.

Ignored local packets contain the detailed receipts and screenshots:

- `.zig-cache/wiki-site-macos-development-20260906/`
- `.zig-cache/site-browser/local/`
